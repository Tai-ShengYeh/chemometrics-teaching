# Edge-TTS 序列生成 14 段旁白（Tecator PLS 教學影片）
import asyncio, edge_tts
from pathlib import Path
OUT = Path(__file__).parent / "assets" / "narration"; OUT.mkdir(parents=True, exist_ok=True)
VOICE, RATE, PITCH = "zh-TW-YunJheNeural", "-10%", "-2Hz"
SCRIPT = [
    (1,  "上一集，PCA 讓我們看見光譜裡的結構。但如果我們真正想要的，是一個能直接預測脂肪含量的模型呢？這一集，主角是偏最小二乘回歸，PLS。"),
    (2,  "傳統測脂肪，要靠濕化學分析，費時、耗試劑、還會破壞樣本。而近紅外光譜，只要幾秒、完全不破壞樣本。我們的目標很明確：用光譜 X，去預測脂肪含量 y。"),
    (3,  "這是一個監督式問題。我們先準備一批樣本，同時有光譜，也有用標準方法測到的脂肪真值。用它們訓練出一條校正模型；之後新的樣本，只要量光譜，就能算出脂肪。"),
    (4,  "為什麼不直接用 PCA？因為 PCA 只追求抓住光譜本身的變異，那個方向，不一定跟脂肪有關。PLS 不一樣，它同時考慮光譜的變異，和它跟脂肪的相關性，找出真正能拿來預測的方向。"),
    (5,  "這些方向，叫潛在變量 LV。PLS 讓每一個潛在變量，最大化光譜 X 和脂肪 y 之間的共變異。一句話：PCA 看 X 自己，PLS 一手抓 X、一手抓 y。"),
    (6,  "實作上，我們先對光譜做 SNV 和一階導數，凸顯吸收峰。再把 240 個樣本切成兩份：180 個拿來訓練校正，60 個完全留到最後測試，模擬沒看過的新樣本。"),
    (7,  "潛在變量要用幾個？用交叉驗證。隨著潛在變量增加，誤差先快速下降，到第四個之後就幾乎不動了。再加上去，只會讓模型背下雜訊、過度擬合。所以我們選在轉折點：四個潛在變量。"),
    (8,  "成果如何？把預測值對真實值畫出來，校正集和測試集的點，都緊貼在一比一的對角線上。測試集的決定係數 R 平方，高達 0.96。模型從沒看過這 60 個樣本，依然預測得很準。"),
    (9,  "怎麼判斷夠不夠好？RMSEP 是預測誤差，這裡大約 2.8 個百分點；RPD，是誤差相對於樣本變異的倍數，達到 5.2。一般 RPD 大於 3 就算堪用，超過 5，屬於優秀。"),
    (10, "再看模型的回歸係數。最大的尖峰，落在 928 奈米，正是脂肪的 C H 吸收帶。這代表模型不是亂猜，而是真的盯著脂肪的化學訊號在預測。可解釋，才可信。"),
    (11, "這樣的模型，可以裝在生產線上做即時品管，取代費時的實驗室分析。但要注意：校正模型只在它見過的範圍內可靠，遇到新品種、新製程，必須重新驗證，不能直接外推。"),
    (12, "把兩集連起來：光譜先前處理；想探索結構、找異常，用非監督的 PCA；想定量預測，就用監督的 PLS，再用交叉驗證選維度、用獨立測試集驗證。這就是近紅外做食品分析的核心地圖。"),
    (13, "一句話帶走：PLS，一手抓光譜、一手抓答案，把幾秒的掃描，變成可信的數字。"),
    (14, "從看見結構，到預測含量，這就是近紅外光譜在食品分析的威力。接下來，我們會用 Orange Data Mining，不寫一行程式，也能重現這整套流程。"),
]
async def synth(i, text):
    c = edge_tts.Communicate(text, VOICE, rate=RATE, pitch=PITCH)
    await c.save(str(OUT / f"page-{i:02d}.mp3")); print(f"OK page-{i:02d}.mp3")
async def main():
    for i, t in SCRIPT:
        for r in range(3):
            try: await synth(i, t); break
            except Exception as e: print(f"retry {i}: {e}"); await asyncio.sleep(2)
    print("All done.")
if __name__ == "__main__": asyncio.run(main())
