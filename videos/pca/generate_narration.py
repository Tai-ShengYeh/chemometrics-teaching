# Edge-TTS 序列生成 13 段旁白（Tecator PCA 教學影片）
import asyncio, edge_tts
from pathlib import Path
OUT = Path(__file__).parent / "assets" / "narration"; OUT.mkdir(parents=True, exist_ok=True)
VOICE, RATE, PITCH = "zh-TW-YunJheNeural", "-10%", "-2Hz"

SCRIPT = [
    (1,  "近紅外光譜，能在不破壞食品的情況下，幾秒內看穿成分。但一條光譜有上百個數字，我們要怎麼讀懂它？這一集，我們從主成分分析 PCA 開始。"),
    (2,  "這是 Tecator 資料集：240 個肉品樣本，每一個都在 850 到 1050 奈米之間，量了 100 個波長的吸光值。畫出來就像這樣，上百條交纏的曲線。每個樣本，都是 100 維空間裡的一個點，人眼根本無法直接想像。"),
    (3,  "分析前，光譜通常要先前處理。樣本顆粒大小、量測距離的差異，會讓整條基線上下飄移。標準常態變量轉換，SNV，把每一條光譜各自校正，讓真正的化學差異浮現出來。"),
    (4,  "主成分分析的想法很直覺。把所有樣本想成一團散開的點，PCA 要找出這團點最分散的方向，當作第一主成分 PC1；再找一個跟它垂直、第二分散的方向，當作 PC2。新的座標軸，沿著資訊最多的方向。"),
    (5,  "用數學講，PCA 把光譜矩陣 X，拆成分數 T 乘上負荷量 P 的轉置。分數，是每個樣本在新座標軸上的位置；負荷量，告訴我們每個波長怎麼組成這些新方向。一個看樣本，一個看變數。"),
    (6,  "那要保留幾個主成分？看陡坡圖。光是 PC1 就抓住了將近 69% 的變異；加上 PC2 來到 97%；前三個主成分，更高達 99%。原本 100 個波長，幾乎可以濃縮成 3 個數字，資訊卻幾乎沒有損失。"),
    (7,  "把每個樣本畫在 PC1、PC2 上，就是分數圖。每一個點，是一個肉品樣本，顏色代表脂肪含量。神奇的是，PCA 從頭到尾都沒看過脂肪數值，樣本卻沿著 PC1，由脂肪高低排得整整齊齊。結構，自己浮現了。"),
    (8,  "分數圖也是品管的利器。這個虛線橢圓，是 95% 的信賴範圍；落在圈外的樣本，代表它和大多數樣本明顯不同，可能是異常、或量測出錯，值得拉出來檢查。"),
    (9,  "負荷量則把我們帶回化學。PC1 和 PC2 的負荷量，在 930 奈米附近都有明顯起伏，那正是脂肪 C H 鍵的吸收帶。PCA 沒有任何化學知識，卻自己重新發現了脂肪的訊號。"),
    (10, "要記得，PCA 是非監督式的：它只看光譜本身，不需要任何標準答案。它擅長探索資料、分群、找異常；但它不會直接告訴你脂肪是幾趴。"),
    (11, "把流程串起來：原始光譜，先做 SNV 前處理，再用 PCA 拆成分數、負荷量和陡坡圖。分數看分群、負荷量看化學、陡坡圖決定維度。這就是用 PCA 探索食品光譜的完整地圖。"),
    (12, "一句話帶走：PCA，把上百個波長，壓縮成幾個你看得懂的方向。"),
    (13, "想真正預測脂肪含量，就要用到下一支影片的主角，PLS 回歸。"),
]

async def synth(i, text):
    c = edge_tts.Communicate(text, VOICE, rate=RATE, pitch=PITCH)
    await c.save(str(OUT / f"page-{i:02d}.mp3")); print(f"OK page-{i:02d}.mp3")
async def main():
    for i, t in SCRIPT:
        for r in range(3):
            try: await synth(i, t); break
            except Exception as e: print(f"retry {i}: {e}"); await asyncio.sleep(2)
    print("All done.")
if __name__ == "__main__": asyncio.run(main())
